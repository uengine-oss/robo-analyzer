[ TODO 스펙 변경 ]


1. 각 파일별 순차적으로 도는게 아니라 파일별로도 병렬처리하면서 그안에서 청크로 병렬처리하는 이중 병렬처리가 가능한가? 가능하면
각파일별로 AST구조대로 그래프트를 다 만들어둡니다. 

2. 모든파일에 대해서 AST 구조가 다 만들어졋다면 -> 병렬단위로 실제 분석을 시작합니다. 파일 여러개씩.. 

3. 단! 사이퍼쿼리 실행 동시성 문제는 적절히 해결하도록 처리가 필요함 

4. 지금 메서드 콜 호출 로직이 패턴으로 식별하도록 되어있는데, 메서드 콜 호출 처리 프롬포트랑 그냥 일반 ANALYSIS 프롬포트랑 병합해서
CALL 배열로 둬서 처리하도록 하고 타겟.메서드명 이런식으로 응답을 뱉어서 . 단위로 때어내서 타겟검사 메서드명 검사하는 식으로 하는게 어떤지?
즉 AST로 다 틀이 만들어졌다면 클래스도 존재할거고, 메서드도 이미 존재할거니까 이름으로 조회해서 바로 연결하면되잖아요 
지금 클래스노드 CALL 노드 등등 임시노드로처리하고 실제 분석될때 해당 타입으로 대체하고있는데 이럴 필요가없다는거임 이해가 되나요?

5. 즉 지금처럼 AST로 다 처리가 되었다면 굳이 MERGE 방식으로 하지않거나 MATCH로 해도된다는것이며, 4번같은 케이스가 없는지 검토후 싹다 갈아엎어주세요 , DBMS나 FRAMEWORK나 등등...

6. 혹시, 스트림으로 줄때, NEO4J에 사이퍼쿼리를 실행하고, 그 결과를 보내줄때 그 결과를 메시지화에서 어떤 관계노드가 생성되는건지를 메시지화해서 보내줄수없다 이런 로직은 UTILS_TOOL 에 함수화해서 , 이방법 말고 더 효율적인 방법이 있나? (나는 어떤식으로 진행중인지가 아니라 어떤 결과가 생성되었는지도 사용자에게 알리고싶다. 어떻게 노드가 추가되었고 관계등도.. 테이블도 어떻게 보강되는지 등등 그래프가 워낙 커서 사용자가 어디를 주시해야하는지 모를테니 로그라도 알려줘야함 )
