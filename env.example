# ==========================================
# ROBO Analyzer 환경변수 설정
# ==========================================
# 이 파일을 .env로 복사하여 값을 설정하세요.
# cp env.example .env
# ==========================================

# ------------------------------------------
# 필수 설정
# ------------------------------------------

# Neo4j 데이터베이스
NEO4J_URI=bolt://127.0.0.1:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=your_password

# OpenAI API 키 (필수)
LLM_API_KEY=sk-your-api-key-here

# ------------------------------------------
# LLM 설정 (선택 - 기본값 사용 가능)
# ------------------------------------------
# LLM_API_BASE=https://api.openai.com/v1
# LLM_MODEL=gpt-4.1
# LLM_MAX_TOKENS=32768
# LLM_CACHE_ENABLED=true

# ------------------------------------------
# 메타데이터 보강 (선택)
# ------------------------------------------
# Text2SQL API로 샘플 데이터 조회하여 테이블/컬럼 설명 생성
# 벡터라이징 전에 실행됩니다.

# TEXT2SQL_API_URL=http://localhost:8001

# FK 관계 추론 설정
# FK_INFERENCE_ENABLED=true
# FK_SAMPLE_SIZE=25
# FK_SIMILARITY_THRESHOLD=0.8
# FK_MATCH_RATIO_THRESHOLD=0.8

# ------------------------------------------
# 병렬 처리 (선택 - 기본값: 5)
# ------------------------------------------
# 대부분의 경우 기본값으로 충분합니다.
# MAX_CONCURRENCY=5
# FILE_CONCURRENCY=5

# ------------------------------------------
# 서버 설정 (선택)
# ------------------------------------------
# HOST=0.0.0.0
# PORT=5502

# ------------------------------------------
# 테스트 설정
# ------------------------------------------
# TEST_DB_NAME=test
# TEST_LOCALE=ko
# TEST_DBMS=postgres
