# ==========================================
# ROBO Analyzer 환경변수 설정 예제
# 이 파일을 .env로 복사하여 값을 설정하세요.
# ==========================================

# ------------------------------------------
# Neo4j 데이터베이스 연결
# ------------------------------------------
NEO4J_URI=bolt://127.0.0.1:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=neo4j

# ------------------------------------------
# LLM API 설정
# ------------------------------------------
# OpenAI 호환 API 엔드포인트
LLM_API_BASE=https://api.openai.com/v1

# API 키 (필수)
LLM_API_KEY=your-api-key-here

# 사용할 모델
LLM_MODEL=gpt-4.1

# 최대 토큰 수
LLM_MAX_TOKENS=32768

# 추론 모델(gpt-5, o1 등) 사용 시 reasoning effort 설정
LLM_REASONING_EFFORT=medium

# 커스텀 LLM 사용 여부 (true/false)
IS_CUSTOM_LLM=false

# 커스텀 LLM 회사명 (IS_CUSTOM_LLM=true일 때 사용)
COMPANY_NAME=

# ------------------------------------------
# 병렬 처리 설정
# ------------------------------------------
# 파일 단위 병렬 처리 동시 실행 수
FILE_CONCURRENCY=5

# 청크/배치 단위 병렬 처리 동시 실행 수
MAX_CONCURRENCY=5

# Framework 분석 전용 병렬 처리
FRAMEWORK_MAX_CONCURRENCY=5
INHERITANCE_CONCURRENCY=5
FIELD_CONCURRENCY=5
METHOD_CONCURRENCY=5

# DBMS 분석 전용 병렬 처리
VARIABLE_CONCURRENCY=5

# ------------------------------------------
# 배치 처리 설정
# ------------------------------------------
# 일반 배치 최대 토큰
MAX_BATCH_TOKEN=1000

# Framework 분석 배치 최대 토큰
FRAMEWORK_MAX_BATCH_TOKEN=1000

# Summary 청크 분할 최대 토큰
MAX_SUMMARY_CHUNK_TOKEN=5000

# ------------------------------------------
# 경로 설정
# ------------------------------------------
# Docker Compose 컨텍스트 경로 (Docker 환경에서만 설정)
# DOCKER_COMPOSE_CONTEXT=/app

# LLM 호출 감사 로그 디렉토리
# LLM_AUDIT_DIR=/app/logs

# ------------------------------------------
# 서버 설정
# ------------------------------------------
HOST=0.0.0.0
PORT=5502

# ------------------------------------------
# 테스트 설정
# ------------------------------------------
TEST_USER_ID=EN_TestSession
TEST_PROJECT_NAME=test
TEST_DB_NAME=test
# TEST_DATA_DIR=/path/to/test/data

