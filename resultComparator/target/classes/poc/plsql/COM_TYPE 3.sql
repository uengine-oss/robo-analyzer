ALTER SESSION SET PLSQL_CODE_TYPE = 'INTERPRETED';

CREATE OR REPLACE PACKAGE COM_TYPE AS


FUNCTION NULL_STR

RETURN VARCHAR2
RESULT_CACHE DETERMINISTIC PARALLEL_ENABLE;

END COM_TYPE;
/


CREATE OR REPLACE PACKAGE BODY COM_TYPE AS

gNullStr CONSTANT VARCHAR2(8) := '['||CHR(15)||'NULL'||CHR(15)||']';

FUNCTION NULL_STR

RETURN VARCHAR2
RESULT_CACHE DETERMINISTIC PARALLEL_ENABLE
IS
BEGIN
	RETURN gNullStr;
END NULL_STR;


END COM_TYPE;
/